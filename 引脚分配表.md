# 智能小车引脚分配表

## STM32F103 引脚分配

### 一、电机驱动（PWM + 方向控制）

| 引脚 | 外设/模式 | 功能 | 说明 |
|---|---|---|---|
| **PA0** | TIM2_CH1（PWM） | 电机 A PWM 输出 | 控制左轮转速，频率 1kHz |
| **PA1** | TIM2_CH2（PWM） | 电机 B PWM 输出 | 控制右轮转速，频率 1kHz |
| **PA6** | GPIO 推挽输出 | 电机 A 方向 | 高=正转，低=反转 |
| **PA7** | GPIO 推挽输出 | 电机 A 方向（反相） | 始终与 PA6 相反，构成 H 桥上下臂 |
| **PB0** | GPIO 推挽输出 | 电机 B 方向 | 高=正转，低=反转 |
| **PB1** | GPIO 推挽输出 | 电机 B 方向（反相） | 始终与 PB0 相反 |

### 二、编码器（速度反馈）

| 引脚 | 外设/模式 | 功能 | 说明 |
|---|---|---|---|
| **PA2** | EXTI2 外部中断（下降沿） | 编码器 A 脉冲计数 | `Encoder_count1`，在中断里 ±1 |
| **PA3** | GPIO 输入上拉 | 编码器 A 方向判断 | 读电平判断正反转 |
| **PA4** | EXTI4 外部中断（下降沿） | 编码器 B 脉冲计数 | `Encoder_count2`，在中断里 ±1 |
| **PA5** | GPIO 输入上拉 | 编码器 B 方向判断 | 读电平判断正反转 |

### 三、8 路灰度传感器（巡线）

| 引脚 | 传感器编号 | 宏定义 | 说明 |
|---|---|---|---|
| **PB12** | D1 | `digtal(1)` | 灰度传感器 1（最左侧） |
| **PB13** | D2 | `digtal(2)` | 灰度传感器 2 |
| **PB14** | D3 | `digtal(3)` | 灰度传感器 3 |
| **PB15** | D4 | `digtal(4)` | 灰度传感器 4（中间偏左） |
| **PA8** | D5 | `digtal(5)` | 灰度传感器 5（中间偏右） |
| **PC13** | D6 | `digtal(6)` | 灰度传感器 6 |
| **PC14** | D7 | `digtal(7)` | 灰度传感器 7 |
| **PC15** | D8 | `digtal(8)` | 灰度传感器 8（最右侧） |

> 全部配置为 GPIO 输入上拉模式。传感器压到黑线时输出低电平（`digtal() == 0`）。

### 四、串口通信

| 引脚 | 外设/模式 | 功能 | 说明 |
|---|---|---|---|
| **PA9** | USART1_TX（复用推挽） | 串口发送 → K230 | 发送扑克牌结果等数据给 K230 |
| **PA10** | USART1_RX（浮空输入） | 串口接收 ← K230 | 接收 W/A/S/D/X/E/P/0~3 指令 |

### 五、蜂鸣器与 LED

| 引脚 | 外设/模式 | 功能 | 说明 |
|---|---|---|---|
| **PB5** | GPIO 推挽输出 | 蜂鸣器 | 高电平响，过圈时触发 |
| **PB6** | GPIO 推挽输出 | LED | 高电平亮，待机时闪烁 |

### 六、OLED 显示屏（软件模拟 I2C）

| 引脚 | 外设/模式 | 功能 |
|---|---|---|
| **PB8** | GPIO 开漏输出 | OLED SCL 时钟线 |
| **PB9** | GPIO 开漏输出 | OLED SDA 数据线 |

### 七、传感器 I2C 总线（软件模拟 I2C）

| 引脚 | 外设/模式 | 功能 |
|---|---|---|
| **PB10** | GPIO 开漏输出 | I2C SCL 时钟线 |
| **PB11** | GPIO 开漏输出 | I2C SDA 数据线 |

> 同一条 I2C 总线上挂载了两个设备：
> - **MPU6050** 六轴传感器（地址 `0xD0`）
> - **HMC5883L** 磁力计（地址 `0x3C`）

### 八、内部定时器（无外部引脚占用）

| 定时器 | 用途 | 周期 |
|---|---|---|
| **TIM2** | 电机 A/B 的 PWM 产生 | 1kHz（占空比范围 0~50000） |
| **TIM3** | 系统主节拍中断 | 10ms，执行 PID 控制 + 里程累计 + 蜂鸣器管理 |

---

## K230 端引脚分配

| K230 引脚 | FPIOA 映射 | 功能 |
|---|---|---|
| **Pin 5** | UART2_TXD | 发送指令到 STM32 |
| **Pin 6** | UART2_RXD | 接收 STM32 返回数据 |

---

## 引脚占用总览（按端口分组）

```
PA0  ─ PWM 电机A
PA1  ─ PWM 电机B
PA2  ─ 编码器A 脉冲
PA3  ─ 编码器A 方向
PA4  ─ 编码器B 脉冲
PA5  ─ 编码器B 方向
PA6  ─ 电机A 方向
PA7  ─ 电机A 方向反相
PA8  ─ 灰度 D5
PA9  ─ UART1 TX
PA10 ─ UART1 RX

PB0  ─ 电机B 方向
PB1  ─ 电机B 方向反相
PB5  ─ 蜂鸣器
PB6  ─ LED
PB8  ─ OLED SCL
PB9  ─ OLED SDA
PB10 ─ I2C SCL（MPU6050 + HMC5883L）
PB11 ─ I2C SDA（MPU6050 + HMC5883L）
PB12 ─ 灰度 D1
PB13 ─ 灰度 D2
PB14 ─ 灰度 D3
PB15 ─ 灰度 D4

PC13 ─ 灰度 D6
PC14 ─ 灰度 D7
PC15 ─ 灰度 D8
```

**未使用端口：** PB2/PB3/PB4/PB7（PB3 和 PB4 是 JTAG 调试口，保留未用）、PC0~PC12。
