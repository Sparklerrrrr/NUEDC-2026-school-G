# car_example 目录结构解读

一个 STM32 嵌入式工程通常按**分层架构**组织代码，`car_example` 下有 4 个文件夹，各司其职。

---

## 一、`sys/` — System（芯片系统文件）

**来源：** ST 官方提供的 CMSIS（Cortex Microcontroller Software Interface Standard）标准文件。

**作用：** 这是程序运行的"地基"，定义了芯片的一切底层细节。**不能改也不该改。**

| 文件 | 作用 |
|---|---|
| `stm32f10x.h` | STM32F103 寄存器地址和位定义，所有外设的寄存器宏都在这 |
| `core_cm3.h` | ARM Cortex-M3 内核寄存器（NVIC 中断控制器、SCB 系统控制块、SysTick） |
| `core_cmFunc.h` / `core_cmInstr.h` | 内核汇编指令封装（开关中断、等待指令、内存屏障等） |
| `system_stm32f10x.c/.h` | 系统时钟初始化，上电后配置 HSE/PLL 把主频拉到 72MHz |
| `startup_stm32f10x_hd.s` / `_md.s` | 启动汇编：上电第一条代码，初始化堆栈、建立中断向量表、跳转到 `main()` |

**为什么叫 sys：** Keil 官方工程模板的习惯——把和芯片强相关的底层文件归到 `system` 目录。

---

## 二、`ml_libs/` — MengLong Libraries（猛龙外设驱动库）

**来源：** 猛龙开发板厂商提供的硬件抽象库。

**作用：** 把寄存器操作包装成易懂的函数，让上层代码不用翻数据手册。

| 文件 | 封装的外设 |
|---|---|
| `ml_gpio.c/.h` | GPIO 引脚输入输出 |
| `ml_uart.c/.h` | 串口收发 |
| `ml_pwm.c/.h` | PWM 输出 |
| `ml_tim.c/.h` | 定时器 |
| `ml_adc.c/.h` | 模数转换 |
| `ml_exti.c/.h` | 外部中断 |
| `ml_nvic.c/.h` | 中断优先级管理 |
| `ml_i2c.c/.h` | I2C 总线 |
| `ml_delay.c/.h` | 微秒/毫秒延时 |
| `ml_oled.c/.h` | OLED 显示屏驱动 |
| `ml_mpu6050.c/.h` | 六轴陀螺仪加速度计 |
| `ml_hmc5883l.c/.h` | 磁力计（电子罗盘） |

**命名规律：** `ml_` 是厂商前缀，后面跟外设名，每个外设一个 `.c` + `.h`。

**为什么叫 libs：** libraries 的缩写，表示"可复用的封装库"——这些文件不包含业务逻辑，只负责硬件操作。

---

## 三、`code/` — 用户算法与业务逻辑

**来源：** 开发者自己写的代码。

**作用：** 这是小车的"大脑"，包含所有决策逻辑和控制算法。和硬件无关，理论上可以移植到任何平台。

自带的 `readme.txt` 写着：**"本文件夹用于存放用户自己的代码"**。

| 文件 | 类型 | 说明 |
|---|---|---|
| `motor.c/.h` | 设备逻辑 | 把 PWM + GPIO 封装成"左轮多少速度、右轮多少速度"的语义 |
| `pid.c/.h` | 通用算法 | PID 控制器（增量式/位置式），纯数学计算，不碰任何寄存器 |
| `gray_track.c/.h` | 业务逻辑 | 8 路灰度巡线策略（PD 控制 + 查表法两套算法） |
| `filter.c/.h` | 通用算法 | Mahony 互补滤波 + Kalman 滤波，传感器数据融合 |
| `buzzer.c/.h` | 设备逻辑 | 蜂鸣器和 LED 的开关封装 |

**和 `ml_libs` 的关键区别：**

| | `ml_libs` | `code` |
|---|---|---|
| 关心什么 | "怎么让 PWM 输出 50% 占空比" | "小车应该以多大速度转弯" |
| 依赖硬件 | 强依赖特定芯片 | 不直接碰寄存器 |
| 能复用吗 | 只适用于同系列 MCU | 算法可以拿到任何平台用 |

---

## 四、`user/` — 应用程序入口 + 工程配置

**来源：** Keil 工程模板 + 开发者自己编写。

**作用：** 程序的"入口大厅"——`main()` 函数在这里，初始化一切，然后进入主循环。也是 Keil 工程文件的存放位置。

### 源代码

| 文件 | 说明 |
|---|---|
| `main.c` | 主函数入口，初始化所有模块后进入死循环，根据模式调度巡线/显示 |
| `isr.c` | 所有中断服务函数集中管理（定时器、编码器计数、串口接收指令） |

### Keil 工程文件

| 文件 | 说明 |
|---|---|
| `Project.uvprojx` | 工程主文件：源文件列表、编译选项、链接脚本、芯片型号 |
| `Project.uvoptx` | 工程选项：调试器类型（J-Link / ST-Link）、下载算法、编辑器设置 |
| `Project.uvguix.*` | 用户会话：断点位置、打开的文件标签、窗口布局，每个开发者一个 |

### IDE 辅助

| 内容 | 说明 |
|---|---|
| `.vscode/` | VS Code 的 C/C++ 配置（IntelliSense 路径等），配合 Keil Assistant 插件在 VS Code 里开发 |
| `DebugConfig/` | 调试目标芯片的配置文件（不同 Flash/RAM 大小的型号） |

### 编译产物

| 内容 | 说明 |
|---|---|
| `Objects/` | 编译中间文件（`.o` `.crf` `.d`），每个 `.c` 文件对应一组，删了就重新编译 |
| `Listings/` | 编译最终产物：`.map`（内存地址分布表）和 `.lst`（汇编 + 机器码对照），用于分析代码体积、找内存溢出 |

---

## 五、四层架构总览

```
┌────────────────────────────────────────────┐
│  user/                                     │
│  main.c  程序入口，初始化一切               │
│  isr.c   中断服务                           │
│                                            │
│  作用：定义"程序要做什么"                    │
└──────────────────┬─────────────────────────┘
                   │ 调用
┌──────────────────▼─────────────────────────┐
│  code/                                     │
│  motor.c  pid.c  gray_track.c  filter.c    │
│                                            │
│  作用：实现"怎么算、怎么决策"                │
│  特点：不碰寄存器，纯逻辑 + 算法             │
└──────────────────┬─────────────────────────┘
                   │ 调用
┌──────────────────▼─────────────────────────┐
│  ml_libs/                                  │
│  ml_gpio  ml_uart  ml_pwm  ml_tim  ml_i2c  │
│                                            │
│  作用：封装"怎么操作硬件"                    │
│  特点：一函数一操作，屏蔽寄存器细节           │
└──────────────────┬─────────────────────────┘
                   │ 操作
┌──────────────────▼─────────────────────────┐
│  sys/                                      │
│  stm32f10x.h  core_cm3.h  startup_*.s      │
│                                            │
│  作用：定义"芯片有什么"                      │
│  特点：官方文件，不可修改                     │
└────────────────────────────────────────────┘
```

**核心原则：上层依赖下层，下层不感知上层。**

这意味着：
- 换一块 STM32 开发板 → 只换 `ml_libs`
- 换一个芯片系列（如换到 STM32F4） → 换 `sys` + `ml_libs`
- 把巡线算法用到树莓派小车 → `code` 直接拿走，改底层调用即可
