# G题：智能识别小车 项目分析

## 项目概述

这是一个**双芯片协作的智能小车**项目：K230 AI 芯片负责视觉识别与跟随决策，STM32F103 负责底层运动控制，二者通过 UART 串口协同工作。

---

## 一、`main.py` — K230 视觉智能小车控制程序

**平台：** K230 AI 芯片（Canaan），MicroPython + nncase 推理框架

**功能：** 摄像头采集图像 → YOLOv5n 人体检测 → 串口向 STM32 发送运动指令，实现**视觉跟随**。

### 三个核心类

| 类 | 作用 |
|---|---|
| `PersonDetectionApp` | AI推理：图像预处理 → YOLOv5n 推理 → anchor-based NMS 后处理 → OSD 绘制检测框 |
| `UARTComm` | 串口通信：FPIOA 映射引脚5/6为 UART2_TXD/RXD，发送单字符指令 |
| `VisionFollow` | 跟随决策：选离画面中心最近的目标，按水平偏差发 W/A/D/X 指令 |

### 跟随控制逻辑

- 画面中心 `(640, 360)`，偏差阈值 `100px`
- 中心附近 → `W` 前进
- 右侧偏移 → `D` 右转
- 左侧偏移 → `A` 左转
- 无目标 → `X` 停止
- 启动时发送 `3` 切入视觉模式

### 检测框过滤策略

- 高度小于屏幕宽度10%的框忽略（远处小目标）
- 靠近边缘且宽度不足的框忽略（部分可见目标）
- 使用 YOLOv5n 模型 + 20%置信度阈值 + 60% NMS 阈值

---

## 二、`car_example/` — STM32F103 智能小车嵌入式固件

**平台：** STM32F103（Cortex-M3），Keil MDK + 标准外设库

### 目录结构

```
car_example/
├── user/main.c          # 主程序、OLED显示、圈数管理
├── user/isr.c           # 中断服务（定时器、编码器、串口接收）
├── code/motor.c/.h      # 电机驱动、编码器、里程计算
├── code/pid.c/.h        # PID控制器（增量式/位置式）、模式管理
├── code/gray_track.c/.h # 8路灰度巡线（PD + 查表法）
├── code/filter.c/.h     # Mahony互补滤波 + Kalman滤波
├── code/buzzer.c/.h     # 蜂鸣器 + LED 控制
├── ml_libs/             # 底层驱动库（GPIO/UART/PWM/TIM/I2C/OLED/MPU6050/HMC5883L）
└── sys/                 # CMSIS 系统文件 + 启动汇编
```

### 四种工作模式

| 模式 | 值 | 说明 |
|---|---|---|
| `MODE_IDLE` | 0 | 空闲，电机停转，等待指令 |
| `MODE_BASIC` | 1 | 基础巡线，跑 **2 圈**后自动停止 |
| `MODE_MANUAL` | 2 | 手动遥控模式 |
| `MODE_VISION` | 3 | 视觉模式：巡线 + 接受 K230 指令，跑 **1 圈** |

### 各模块详解

#### 1. 电机与编码器 (`motor.c`)

- PWM + GPIO 方向控制，左右轮独立驱动
- 两路编码器（PA2/PA3、PA4/PA5），外部中断计数
- 里程计算：基于脉冲数 × 轮周长（直径 6.5cm → 周长约 20.4cm）
- 支持**开环直接控制**（`motor_direct_set`）和 **PID 闭环控制**（`motor_target_set`）

#### 2. PID 控制器 (`pid.c`)

- **增量式 PID**（`DELTA_PID`）和**位置式 PID**（`POSITION_PID`）双模式
- 主函数以增量式 PID 初始化（P=10, I=10, D=5）
- `pid_control()` 在 **10ms 定时中断**中执行：
  1. 读取编码器计数作为速度反馈
  2. 累加里程和圈数
  3. PID 计算并限幅
  4. 更新 PWM 占空比

#### 3. 灰度巡线 (`gray_track.c`)

**两套算法：**

- **`track_pid()` — 加权平均 + PD 控制：**
  - 8 路灰度加权值：`[-7, -5, -3, -1, 1, 3, 5, 7]`
  - 偏差 = 加权和 / 检测到线的传感器数
  - PD 修正（Kp=2000, Kd=800），基础占空比 20000
  - 丢线处理：根据上次偏差方向原地旋转找线，超过 100 次丢线则直行寻线

- **`track()` — 查表法：**
  - 根据 D1~D8 具体组合直接查表给出左右轮速度差
  - 越偏外侧权重越大（最大 40:180）

#### 4. 姿态滤波 (`filter.c`)

- **Mahony 互补滤波**：陀螺仪 + 加速度计数据融合（α=0.95238）
- **Kalman 滤波**：Yaw / Roll / Pitch 三轴独立卡尔曼滤波
- 说明小车配备 **MPU6050 六轴传感器**，具备姿态感知能力

#### 5. UART 串口通信 (`isr.c`)

K230 通过 UART1 向 STM32 发送单字符指令：

| 指令 | 作用 |
|---|---|
| `W` | 前进（Vision 模式） |
| `S` | 后退（Vision 模式） |
| `A` | 左转（Vision 模式） |
| `D` | 右转（Vision 模式） |
| `X` | 停车 |
| `E` | 紧急停止 + 切回空闲 |
| `0` | 切换到 MODE_IDLE |
| `1` | 切换到 MODE_BASIC |
| `2` | 切换到 MODE_MANUAL |
| `3` | 切换到 MODE_VISION |
| `P + suit + rank` | 扑克牌识别结果（花色 + 点数），显示在 OLED |

#### 6. OLED 显示 (`main.c`)

每 10 帧刷新一次，显示内容：

| 行 | 内容 |
|---|---|
| 第1行 | 模式（IDLE/BASIC/MAN/VIS）+ 圈数 |
| 第2行 | 运行时间（MM:SS）+ 总里程（cm） |
| 第3行 | 当前速度 |
| 第4行 | 扑克牌识别结果（花色 + 点数，有结果时才显示） |

#### 7. 圈数管理 (`check_lap`)

- 轨道周长：`314 cm`（周长宏 `TRACK_CIRCUMFERENCE_CM`）
- 累加里程达到周长时计一圈，蜂鸣器响 + LED 闪烁
- BASIC 模式 2 圈 / VISION 模式 1 圈后自动停车

---

## 三、系统协作流程

```
┌──────────────────────────────────┐
│  K230（main.py）                  │
│  ├─ 摄像头采集 1280×720 图像      │
│  ├─ YOLOv5n 人体检测推理          │
│  ├─ 视觉跟随决策                  │
│  │   ├─ 选最近中心的目标          │
│  │   └─ 计算水平偏差 → 方向指令   │
│  └─ UART2 发送单字符指令 ─────┐   │
└───────────────────────────────│───┘
                                │
                    UART 串口 (W/A/D/X/0~3/E/P)
                                │
┌───────────────────────────────│───┐
│  STM32F103（car_example）     ◄───┘
│  ├─ UART1 中断接收指令         │
│  ├─ 模式管理（4种模式切换）     │
│  ├─ 8路灰度巡线（PD/查表）      │
│  ├─ PID 电机闭环控制（10ms）    │
│  ├─ 编码器里程 / 圈数统计       │
│  ├─ OLED 实时状态显示           │
│  ├─ MPU6050 姿态感知            │
│  └─ 扑克牌识别结果展示          │
└──────────────────────────────────┘
```

### 典型工作流程

1. **上电** → STM32 初始化为 MODE_IDLE，OLED 显示 "Waiting..."
2. **K230 发送 `3`** → 切换到 MODE_VISION 视觉模式
3. **STM32 巡线** → `track_pid()` 维持沿轨道行驶
4. **K230 检测到人** → 发送 W/A/D 指令引导小车靠近
5. **过圈** → 蜂鸣器响，LED 闪，圈数+1
6. **跑满圈数** → 自动停车切回 MODE_IDLE
7. **K230 检测到扑克牌** → 发送 `P + 花色 + 点数`，OLED 显示结果

---

## 四、关键技术参数

| 参数 | 值 |
|---|---|
| AI 模型 | YOLOv5n（人体检测） |
| 输入分辨率 | 640×640 |
| 显示分辨率 | 1280×720 |
| 置信度阈值 | 0.2 |
| NMS 阈值 | 0.6 |
| 电机 PWM 频率 | 1 kHz |
| PID 控制周期 | 10 ms |
| PID 类型 | 增量式（P=10, I=10, D=5） |
| 巡线 PD | Kp=2000, Kd=800 |
| 轮径 | 6.5 cm |
| 编码器 PPR | 20 |
| 轨道周长 | 314 cm |
| BASIC 圈数 | 2 圈 |
| VISION 圈数 | 1 圈 |
| 串口波特率 | 115200 (UART1, K230→STM32) |
| 主控 MCU | STM32F103（Cortex-M3） |
| AI 芯片 | K230（Canaan） |
| 传感器 | 8路灰度、MPU6050六轴、HMC5883L磁力计 |
